spring.application.name=backend

# ── Servidor ──
server.port=9090

# ── Recursos estáticos ──
spring.web.resources.static-locations=classpath:/static/
spring.web.resources.cache.period=3600

# ── Deshabilitar autoconfiguración de JPA/DataSource hasta configurar MySQL ──
# (sin efecto ahora que JPA no está en el pom, se puede dejar como referencia)
# spring.autoconfigure.exclude=...

# ── Base de datos (descomentar cuando MySQL esté disponible) ──
# spring.datasource.url=jdbc:mysql://localhost:3306/redmatrix_db?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
