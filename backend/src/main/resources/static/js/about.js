/**
 * ABOUT.JS — RedMatrixSolutions
 * Animaciones de la sección Quiénes Somos
 */

'use strict';

(function initAbout() {
  const section = document.querySelector('.about');
  if (!section) return;

  // Las animaciones fade-in son manejadas por el IntersectionObserver global en main.js
  // Este módulo puede extenderse para lógica específica de la sección

})();
